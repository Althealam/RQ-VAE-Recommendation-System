# 忽略Python编译产生的字节码文件
*.pyc

# 忽略数据文件（如果这些分词、清理后的中间数据或最终保存的数据不需要版本控制）
*.csv
*.pkl

# 忽略虚拟环境目录（如果有，比如你的 rqvae 虚拟环境）
rqvae/

# 忽略日志文件（如果后续有生成日志的话）
*.log

# 忽略 macOS 系统特定的隐藏文件
.DS_Store

# 忽略 IDE 相关的配置文件（比如 PyCharm、VS Code 等生成的）
.idea/
.vscode/

# 忽略数据目录下可能存在的临时文件或大文件（如果有单独的数据目录，比如你代码里的 ml-1m 原始数据和预处理后的数据，按需决定是否忽略原始数据，这里先假设原始数据不需要上传，预处理后的数据也可根据需求调整）
data/ml-1m/
data/preprocessed_data/